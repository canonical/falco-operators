name: Tests

on:
  pull_request:

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@main
    permissions:
      contents: read
      pull-requests: write
    secrets: inherit
    strategy:
      matrix:
        charm: ["falco", "falcosidekick-k8s"]
    with:
      with-uv: true
      working-directory: './${{ matrix.charm }}-operator'
